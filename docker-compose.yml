version: '3.8'

services:
  luna-site-dashboard:
    build: .
    container_name: luna-site-dashboard
    ports:
      - "8501:8501"
    volumes:
      - ./code:/app/code
    environment:
      - PYTHONUNBUFFERED=1
